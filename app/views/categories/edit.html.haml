%ul.breadcrumb#breadcrumb
  %li
    %a{href: root_path}
      %i.icon-home
    %span.divider •
  %li
    %a{href: categories_path}
      %i.icon-tags
      = t "application.nav.categories"
    %span.divider •
  - @category.ancestors.reverse.each do |k|
    %li
      %a{href: category_path(k)}= k.name
      %span.divider •
  %li.active= @category.name

%h1
  Edit
  = @category.name

= render partial: "form"
