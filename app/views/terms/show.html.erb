<header class="page-header">
  <h1>
    <%= t(@document.title) %>
  </h1>
</header>

<%= m t(@document.content) %>

<%= form_tag accept_terms_path, method: :post do %>
  <button class="btn" type="submit">
    <%= t '.accept' %>
  </button>
<% end %>
