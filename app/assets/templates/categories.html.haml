%table.table.table-hover.table-condensed
  %thead
    %tr
      %th{:colspan => "10"}
        .navbar
          .navbar-inner
            %ul.nav.pull-right
            %a.btn.btn-primary{:href => "/categories/new"} New category
    %tr
      %th name
  %tbody
    %tr{"ng-repeat" => "cat in ( categories | filter:searchTerm | orderBy:'fqn' )"}
      %td
        %a{:href => "/categories/{{ cat.id }}"} {{ cat.fqn }}